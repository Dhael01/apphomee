{
  "name": "nuestro-hogar",
  "version": "1.0.0",
  "description": "App de gestión del hogar compartido",
  "private": true,
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "devDependencies": {
    "sharp": "^0.33.4"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
