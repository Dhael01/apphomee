{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability por defecto — permite abrir URLs externas (Supabase)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
